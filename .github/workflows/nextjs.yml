# Sample workflow for building and deploying a Next.js site to GitHub Pages
#
# To get started with Next.js see: https://nextjs.org/docs/getting-started
#
name: Deploy Next.js site to Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          static_site_generator: next
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            dsci-project/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('dsci-project/yarn.lock') }}-${{ hashFiles('dsci-project/**.[jt]s', 'dsci-project/**.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('dsci-project/yarn.lock') }}-
      - name: Install dependencies
        working-directory: ./dsci-project
        run: yarn install
      - name: Build with Next.js
        working-directory: ./dsci-project
        run: yarn next build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dsci-project/out
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
